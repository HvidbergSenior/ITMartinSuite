<div class="main-category-box card-secondary @CssClass @(IsLocked ? "category-locked" : "category-active")"
     @onclick="HandleClick">

    <strong>@Title</strong>

    <!-- 🔥 STATUS -->
    <div class="category-status mt-1">

        @if (Total == 0)
        {
            <span class="text-muted">No files</span>
        }
        else if (ReviewCount == 0)
        {
            <span class="text-success">✔ All done</span>
        }
        else
        {
            <div class="text-warning fw-semibold">
                ⚠ @ReviewCount need review
            </div>

            @if (ProcessedCount > 0)
            {
                <div class="text-muted small">
                    @ProcessedCount already processed
                </div>
            }
        }

    </div>

    <!-- 📊 TOTAL -->
    <div class="category-meta mt-1">
        @Total files • @SizeText
    </div>

    <!-- 👉 ACTION -->
    @if (!IsLocked && ReviewCount > 0)
    {
        <div class="category-action mt-2">
            ▶ Start Review
        </div>
    }

</div>
@code {
    [Parameter] public string Title { get; set; } = "";

    // 🔥 ONLY ONE NUMBER THAT MATTERS
    [Parameter] public int ReviewCount { get; set; }

    [Parameter] public int Total { get; set; }
    [Parameter] public string SizeText { get; set; } = "";

    [Parameter] public string CssClass { get; set; } = "";
    [Parameter] public bool IsLocked { get; set; }
    [Parameter] public EventCallback OnClick { get; set; }
    [Parameter] public int ProcessedCount { get; set; }
    private async Task HandleClick()
    {
        if (!IsLocked)
            await OnClick.InvokeAsync();
    }
}