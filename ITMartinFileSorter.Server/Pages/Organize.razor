@page "/organize"

@using ITMartinFileSorter.Domain.Entities
@using ITMartinFileSorter.Domain.Enums

@inject DuplicateService DuplicateService
@inject NavigationManager Navigation

<div class="filesorter-app">
    <div class="page-container stack organize-page">

        <SectionTitle Title="🗂 Organize Files" />

        <!-- FILTER -->
        <ContentCard Title="Filter">
            <select @bind="SelectedCategory">
                <option value="">All categories</option>
                <option value="Image">Images</option>
                <option value="Video">Videos</option>
                <option value="Audio">Audio</option>
                <option value="Document">Documents</option>
            </select>
        </ContentCard>

        <!-- FILES -->
        <div class="organize-grid">
            @foreach (var file in FilteredFiles)
            {
                <div class="organize-file-card">

                    <div class="row space-between">

                        <div class="stack">

                            <div class="item-title">@file.FileName</div>

                            <div class="organize-meta">

                                <div>
                                    <span class="item-label">Type:</span>
                                    @file.MainCategory
                                </div>

                                <div>
                                    <span class="item-label">Date:</span>
                                    @(file.CreatedAt?.ToString("yyyy-MM-dd") ?? "Unknown")
                                </div>

                                <div>
                                    <span class="item-label">Status:</span>
                                    @file.Status
                                </div>

                            </div>

                        </div>

                        <input type="checkbox"
                               checked="@IsSelected(file)"
                               @onchange="e => Toggle(file, (bool)e.Value!)" />

                    </div>

                </div>
            }
        </div>

        <!-- CTA -->
        <ActionButtonBar CssClass="bottom">
            <button class="btn btn-success btn-lg"
                    @onclick="GoToExport">
                Continue to Export
            </button>
        </ActionButtonBar>

    </div>
</div>

@code {
    string SelectedCategory = "";

    HashSet<MediaFile> Selected = new();

    IEnumerable<MediaFile> FilteredFiles =>
        string.IsNullOrWhiteSpace(SelectedCategory)
            ? DuplicateService.AllFiles.Where(f => f.Status == MediaFileStatus.ToKeep)
            : DuplicateService.AllFiles.Where(f =>
                f.Status == MediaFileStatus.ToKeep &&
                f.MainCategory.ToString() == SelectedCategory);

    bool IsSelected(MediaFile file) => Selected.Contains(file);

    void Toggle(MediaFile file, bool value)
    {
        if (value)
            Selected.Add(file);
        else
            Selected.Remove(file);
    }

    void GoToExport()
    {
        Navigation.NavigateTo("/export");
    }
}